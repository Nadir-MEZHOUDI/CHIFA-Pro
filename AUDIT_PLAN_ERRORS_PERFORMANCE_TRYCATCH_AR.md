# خطة تدقيق CHIFA Pro (الأخطاء، الأداء، وتغطية Try-Catch)

التاريخ: 2026-03-16
النطاق: تدقيق ساكن متعدد المستويات + تحقق بناء (بدون تعديل كود الإنتاج)
الحالة: الاكتشاف مكتمل، والخطة جاهزة للتنفيذ

## 1) مستويات التدقيق التي تم تنفيذها

1. فحص أمان الاستثناءات
- تمت مراجعة معالجات أحداث الواجهة، حدود `async void`، مسارات الإقلاع/التهيئة، خيوط الخلفية، ومسارات الإنهاء (dispose).

2. فحص مخاطر الأداء
- تمت مراجعة تدفقات تحميل البيانات، أنماط استعلامات قاعدة البيانات، تكرار تحديث الواجهة، استخدام الخيوط، ودورة حياة الموارد.

3. التحقق من البناء والمحللات
- تم تنفيذ restore/build لاكتشاف التحذيرات أو الأخطاء التي قد تخفي مخاطر وقت التشغيل.

## 2) نتائج التحقق عبر الأوامر

- `dotnet restore "CHIFA Pro.sln" -v minimal`: نجاح، 0 تحذيرات، 0 أخطاء.
- `dotnet build "CHIFA Pro.sln" -c Debug -v minimal`: نجاح، 2 تحذيرات، 0 أخطاء.
- التحذيرات الحالية: ترخيص DevExpress فقط (`DX1000`, `DX1001`) من `CHIFA Pro/CHIFA.Pro.csproj`.
- لم تظهر تحذيرات Nullable (`CS86xx`) أو Async أو Dispose أو Threading في نمط البناء هذا.

## 3) أعلى الأولويات (تُعالج أولاً)

### أ) نقص try-catch في مسارات UI غير متزامنة عالية الخطورة

1) `CHIFA Pro/Views/OfficineUC.cs:13`
- معالج تحميل `async void` ينتظر استدعاء قاعدة بيانات بدون `try-catch` محلي.
- الخطر: استثناء غير معالج قد يوقف تدفق الواجهة عند أخطاء الوصول للبيانات.

2) `CHIFA Pro/Views/NomenclaturUC.cs:21`
- معالج تغيّر التركيز في الـGrid (`async void`) يستدعي قاعدة بيانات بدون حماية.
- الخطر: تغيّر التركيز المتكرر قد يُظهر أعطال غير معالجة.

3) `CHIFA Pro/Views/frmHistory.cs:20` و `CHIFA Pro/Views/frmHistory.cs:76`
- مسار التحميل ينتظر دوال تحميل بيانات بينما أحد المسارات لا يملك معالجة محلية للاستثناء.
- الخطر: الاستثناءات وقت التحميل قد تتسرب عبر حد `async void`.

### ب) بطء واجهة السيرفر وضغط الذاكرة

4) `CHIFA.Server/Views/MainWindow.xaml.cs:45`
- حلقة خلفية لا نهائية تحدّث السجلات كل 500ms وتعيد تحميل النص كاملًا في كل دورة.
- الخطر: ازدياد استهلاك الذاكرة وبطء الواجهة مع طول التشغيل.

5) `CHIFA.Server/App.xaml.cs:14`
- `StringWriter` داخل الذاكرة يحتفظ بالسجلات بدون حد أقصى.
- الخطر: الجلسات الطويلة تتدهور تدريجيًا.

### ج) تحميل كميات كبيرة في الذاكرة قبل التجميع

6) `CHIFA.DAL/DataServices/ChifaService.cs:377`, `:396`, `:399`, `:445`, `:464`
7) `CHIFA.DAL/DataServices/StatisticsService.cs:195`, `:216`
- يتم تحميل بيانات كبيرة ثم تطبيق التجميع داخل الذاكرة في عدة مسارات.
- الخطر: استهلاك CPU/RAM مرتفع وبطء ملحوظ مع قواعد بيانات أكبر.

## 4) ملاحظات متوسطة الأولوية

1) صلابة الإقلاع والإنهاء
- `CHIFA Pro/Program.cs:35`, `CHIFA.Server/App.xaml.cs:24`: مسارات الإقلاع تحتاج حاوية أعطال أعلى مستوى.
- `CHIFA.Server/Helpers/GrpcServer.cs:86`: ترتيب dispose قد يتسابق مع مسار stop غير المتزامن.

2) عواصف استعلامات من أحداث الواجهة
- `CHIFA Pro/Views/FacturesUC.cs:87`, `:88`, `:136`
- `CHIFA Pro/Views/TraitSpecUc.cs:46`, `:47`, `:118`
- `CHIFA Pro/Views/StatisticsUc.cs:31`, `:37`, `:185`, `:186`
- الخطر: أحداث سريعة متتالية تُطلق تحميلات قاعدة بيانات متداخلة.

3) أعمال حابسة في مسار الإقلاع
- `CHIFA Pro/Views/HomeUc.cs:124`, `CHIFA Pro/Helpers/DbChecker.cs:90`, `:72`
- الخطر: تأخير/تجميد عند الإقلاع بسبب انتظار عمليات متزامن قبل أول `await`.

4) نظافة دورة حياة الموارد
- `CHIFA.DAL/DataServices/ChifaService.cs:344`
- `CHIFA Pro/Views/frmMain.cs:290`
- `CHIFA Pro/Helpers/DbChecker.cs:31`, `:132`
- `CHIFA Pro/Helpers/XtraHelper.cs:129`
- الخطر: ضغط على المقابض/الاتصالات خلال الجلسات الطويلة.

5) تزامن الواجهة وإعادة الدخول
- `CHIFA Pro/Views/frmMain.cs:120`, `:165`, `:335`
- `CHIFA Pro/Helpers/XtraHelper.cs:23`, `:40`
- الخطر: تقطع أو أخطاء متقطعة بسبب `Application.DoEvents()` واختلاط سياق التنفيذ.

## 5) ملخص تغطية Try-Catch

تغطية جيدة نسبيًا:
- عدة شاشات WinForms تتبع نمط `try-catch + ex.Log()` بشكل متسق.

فجوات يجب إغلاقها أولًا:
- معالجات `async void` في `OfficineUC`, `NomenclaturUC`, `frmHistory`, `frmTraitSpec`.
- حلقة الخلفية وحدود الإقلاع/الإنهاء في السيرفر.

## 6) خطة التنفيذ على مراحل

### المرحلة 0 - خط أساس وفحوصات أمان (نصف يوم)
- تثبيت تشغيل baseline قابل للتكرار (restore/build + smoke startup).
- تأكيد إعداد ترخيص DevExpress محليًا وفي CI لتقليل ضجيج التحذيرات.

### المرحلة 1 - منع الأعطال وحدود الاستثناءات (يوم)
- إضافة/توحيد `try-catch` في جميع حدود أحداث `async void` غير المغطاة.
- إضافة تغليف آمن لمسارات إقلاع العميل/السيرفر.
- تقوية حلقة سجلات السيرفر بإلغاء منظم ومعالجة أخطاء التحديث.

معيار القبول:
- عدم وجود استثناءات غير معالجة أثناء التنقل العادي واختبار إقلاع/إغلاق سريع.

### المرحلة 2 - مكاسب أداء سريعة (1-2 يوم)
- استبدال تحديث السجل الكامل بتحديث تدريجي مع Buffer محدود.
- إضافة debounce + cancellation لأحداث إعادة التحميل عالية التكرار.
- إزالة `Task.Run` الزائد حول I/O غير المتزامن.
- إزالة/استبدال `Application.DoEvents()` قدر الإمكان.

معيار القبول:
- انخفاض واضح في التقطيع أثناء الفلترة/التنقل وفي نافذة سجل السيرفر مع الزمن.

### المرحلة 3 - تحسين الوصول للبيانات (2-4 أيام)
- نقل التجميعات الثقيلة من الذاكرة إلى إسقاطات (projections) على مستوى SQL.
- تقليل الأعمدة المختارة وإضافة paging للشاشات الثقيلة.
- قياس عدد الاستعلامات وزمن التنفيذ على بيانات واقعية.

معيار القبول:
- انخفاض طفرات RAM وتحسن زمن التحميل لشاشات الإحصائيات/العلاجات/المنتجات.

### المرحلة 4 - تقوية الموارد والإنهاء (يوم)
- توحيد `using/await using` لاتصالات DB و`Process` و`Ping`.
- إصلاح ترتيب stop/dispose في دورة حياة gRPC server.

معيار القبول:
- استقرار في الجلسات الطويلة بدون ضغط متزايد على المقابض/الاتصالات.

## 7) مصفوفة التحقق بعد التحسينات

- Smoke وظيفي: فتح الشاشات الأساسية، تغيير المرشحات/التواريخ، تنقل متكرر.
- Smoke استقرار: تشغيل العميل/السيرفر لفترة ممتدة ومراقبة الاستجابة.
- ضغط بيانات: اختبار على أحجام بيانات أكبر للتحقق من تحسن الاستعلامات.
- بوابة بناء: استمرار نجاح `dotnet build "CHIFA Pro.sln"`.

## 8) المخاطر والاعتماديات

- تحذيرات ترخيص DevExpress قد تحجب إشارة البناء في CI حتى اكتمال الإعداد.
- تحسينات الاستعلام قد تغيّر ترتيب/شكل النتائج وتحتاج فحص UI انحداري.
- ضبط debounce/cancellation يحتاج موازنة UX لتجنب الإحساس بتأخر الاستجابة.

## 9) ترتيب التنفيذ الموصى به (أعلى عائد)

1. إغلاق فجوات try-catch في حدود async UI.
2. إصلاح حلقة السجل وتقييد الذاكرة في السيرفر.
3. إضافة debounce/cancel لأحداث إعادة التحميل عالية التردد.
4. إزالة انتظار الإقلاع الحابس و`Task.Run` الزائد.
5. نقل التجميع الثقيل إلى SQL مع paging للشاشات الثقيلة.
